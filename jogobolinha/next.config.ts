/**
 * next.config.ts — Configuração do framework Next.js.
 * reactCompiler: ativa o compilador React experimental do projeto.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler: true,
};

export default nextConfig;
